import React from 'react';

const VideoNews = ({video}) => {
    console.log(video);
    //video.map()
    return (
    <div>
        
        {/* <iframe title="COVIDyouTube-video" width="320px" src={video.snippet.thumbnails.default} frameBorder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"></iframe> */}
    </div>);
}

export default VideoNews;